// Generated by Lisette bindgen
// Source: ./testdata/fixtures/structs (local)
// Go: 0.0.0
// Lisette: 0.0.0

import "go:io"

/// Function that returns a function
pub fn MakeAdder(x: int) -> Option<fn(int) -> int>

pub fn ReturnsAnon() -> Unknown

/// Function that takes a callback
pub fn WithCallback(cb: fn(int) -> bool)

/// Struct embedding another struct
pub struct Base {
  pub ID: int,
  pub Name: string,
}

/// Struct with documented fields - tests emitType field doc path
pub struct Documented {
  pub ID: int,
  pub Name: string,
}

/// Empty struct
pub type Empty

pub struct Extended {
  pub Base: Base,
  pub Extra: string,
}

pub struct Form {
  pub Validate: Option<Validator>,
  pub Name: string,
}

pub struct Handler {
  pub OnSuccess: Option<fn(string) -> ()>,
  pub OnError: Option<fn(error) -> ()>,
  pub Transform: Option<fn(Slice<uint8>) -> Result<Slice<uint8>, error>>,
  pub Lookup: Option<fn(string) -> Option<int>>,
  pub GetCoords: Option<fn() -> (float64, float64, float64)>,
}

/// Mixed visibility (unexported fields ignored)
pub struct Mixed {
  pub Public: string,
}

pub struct Node {
  pub Value: int,
  pub Next: Option<Ref<Node>>,
}

/// Basic struct with exported fields
pub struct Point {
  pub X: int,
  pub Y: int,
}

/// Collection-of-pointer fields
pub struct Project {
  pub Name: string,
  pub Modules: Slice<Option<Ref<Node>>>,
  pub Lookup: Map<string, Option<Ref<Node>>>,
}

/// Interface embedding
pub interface ReadWriteCloser {
  fn Close() -> Result<(), error>
  fn Read(mut p: Slice<uint8>) -> Partial<int, error>
  fn Write(p: Slice<uint8>) -> Partial<int, error>
}

/// Shadowing: Shadower declares its own GetID, so Base.GetID should not be promoted
pub struct Shadower {
  pub Base: Base,
}

pub struct Tree {
  pub Left: Option<Ref<Tree>>,
  pub Right: Option<Ref<Tree>>,
  pub Data: string,
}

/// Named function-type alias used as a struct field — same nilability as
/// anonymous func fields
pub type Validator = fn(string) -> Result<(), error>

/// Struct embedding interface
pub struct Wrapper {
  pub Reader: Option<io.Reader>,
  pub Label: string,
}

// SKIPPED field "Nested": anonymous-struct — anonymous struct types are not supported
pub type AnonField_struct

pub var AnonField: AnonField_struct

impl Base {
  /// GetID returns the ID
  fn GetID(self) -> int
  /// SetName sets the name
  fn SetName(self: Ref<Base>, name: string)
}

impl Extended {
  /// GetID returns the ID
  fn GetID(self) -> int
  /// SetName sets the name
  fn SetName(self: Ref<Extended>, name: string)
}

impl Shadower {
  /// GetID overrides the promoted method from Base
  fn GetID(self) -> string
  /// SetName sets the name
  fn SetName(self: Ref<Shadower>, name: string)
}

impl Wrapper {
  fn Read(self, mut p: Slice<uint8>) -> Partial<int, error>
}
