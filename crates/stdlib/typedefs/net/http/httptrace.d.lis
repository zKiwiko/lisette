// Generated by Lisette bindgen
// Source: net/http/httptrace (Go stdlib)
// Go: 1.25.5
// Lisette: 0.1.11

import "go:context"
import "go:crypto/tls"
import "go:net"
import "go:net/textproto"
import "go:time"

pub fn ContextClientTrace(ctx: context.Context) -> Option<Ref<ClientTrace>>

/// WithClientTrace returns a new context based on the provided parent
/// ctx. HTTP client requests made with the returned context will use
/// the provided trace hooks, in addition to any previous hooks
/// registered with ctx. Any hooks defined in the provided trace will
/// be called first.
pub fn WithClientTrace(ctx: context.Context, trace: Ref<ClientTrace>) -> context.Context

/// ClientTrace is a set of hooks to run at various stages of an outgoing
/// HTTP request. Any particular hook may be nil. Functions may be
/// called concurrently from different goroutines and some may be called
/// after the request has completed or failed.
/// 
/// ClientTrace currently traces a single HTTP request & response
/// during a single round trip and has no hooks that span a series
/// of redirected requests.
/// 
/// See https://blog.golang.org/http-tracing for more.
pub struct ClientTrace {
  pub GetConn: fn(string) -> (),
  pub GotConn: fn(GotConnInfo) -> (),
  pub PutIdleConn: fn(error) -> (),
  pub GotFirstResponseByte: fn() -> (),
  pub Got100Continue: fn() -> (),
  pub Got1xxResponse: fn(int, textproto.MIMEHeader) -> Result<(), error>,
  pub DNSStart: fn(DNSStartInfo) -> (),
  pub DNSDone: fn(DNSDoneInfo) -> (),
  pub ConnectStart: fn(string, string) -> (),
  pub ConnectDone: fn(string, string, error) -> (),
  pub TLSHandshakeStart: fn() -> (),
  pub TLSHandshakeDone: fn(tls.ConnectionState, error) -> (),
  pub WroteHeaderField: fn(string, Slice<string>) -> (),
  pub WroteHeaders: fn() -> (),
  pub Wait100Continue: fn() -> (),
  pub WroteRequest: fn(WroteRequestInfo) -> (),
}

/// DNSDoneInfo contains information about the results of a DNS lookup.
pub struct DNSDoneInfo {
  pub Addrs: Slice<net.IPAddr>,
  pub Err: error,
  pub Coalesced: bool,
}

/// DNSStartInfo contains information about a DNS request.
pub struct DNSStartInfo {
  pub Host: string,
}

/// GotConnInfo is the argument to the [ClientTrace.GotConn] function and
/// contains information about the obtained connection.
pub struct GotConnInfo {
  pub Conn: Option<net.Conn>,
  pub Reused: bool,
  pub WasIdle: bool,
  pub IdleTime: time.Duration,
}

/// WroteRequestInfo contains information provided to the WroteRequest
/// hook.
pub struct WroteRequestInfo {
  pub Err: error,
}
