// Generated by Lisette bindgen
// Source: crypto/ed25519 (Go stdlib)
// Go: 1.25.5
// Lisette: 0.1.9

import "go:crypto"
import "go:io"

/// GenerateKey generates a public/private key pair using entropy from rand.
/// If rand is nil, [crypto/rand.Reader] will be used.
/// 
/// The output of this function is deterministic, and equivalent to reading
/// [SeedSize] bytes from rand, and passing them to [NewKeyFromSeed].
pub fn GenerateKey(rand: io.Reader) -> Result<(PublicKey, PrivateKey), error>

pub fn NewKeyFromSeed(seed: Slice<uint8>) -> PrivateKey

/// Sign signs the message with privateKey and returns a signature. It will
/// panic if len(privateKey) is not [PrivateKeySize].
pub fn Sign(privateKey: PrivateKey, message: Slice<uint8>) -> Slice<uint8>

/// Verify reports whether sig is a valid signature of message by publicKey. It
/// will panic if len(publicKey) is not [PublicKeySize].
/// 
/// The inputs are not considered confidential, and may leak through timing side
/// channels, or if an attacker has control of part of the inputs.
pub fn Verify(publicKey: PublicKey, message: Slice<uint8>, sig: Slice<uint8>) -> bool

/// VerifyWithOptions reports whether sig is a valid signature of message by
/// publicKey. A valid signature is indicated by returning a nil error. It will
/// panic if len(publicKey) is not [PublicKeySize].
/// 
/// If opts.Hash is [crypto.SHA512], the pre-hashed variant Ed25519ph is used and
/// message is expected to be a SHA-512 hash, otherwise opts.Hash must be
/// [crypto.Hash](0) and the message must not be hashed, as Ed25519 performs two
/// passes over messages to be signed.
/// 
/// The inputs are not considered confidential, and may leak through timing side
/// channels, or if an attacker has control of part of the inputs.
pub fn VerifyWithOptions(
  publicKey: PublicKey,
  message: Slice<uint8>,
  sig: Slice<uint8>,
  opts: Ref<Options>,
) -> Result<(), error>

/// Options can be used with [PrivateKey.Sign] or [VerifyWithOptions]
/// to select Ed25519 variants.
pub struct Options {
  pub Hash: crypto.Hash,
  pub Context: string,
}

/// PrivateKey is the type of Ed25519 private keys. It implements [crypto.Signer].
pub struct PrivateKey(Slice<uint8>)

/// PublicKey is the type of Ed25519 public keys.
pub struct PublicKey(Slice<uint8>)

const PrivateKeySize = 64

const PublicKeySize = 32

const SeedSize = 32

const SignatureSize = 64

impl Options {
  /// HashFunc returns o.Hash.
  fn HashFunc(self: Ref<Options>) -> crypto.Hash
}

impl PrivateKey {
  /// Equal reports whether priv and x have the same value.
  fn Equal(self, x: crypto.PrivateKey) -> bool

  /// Public returns the [PublicKey] corresponding to priv.
  fn Public(self) -> crypto.PublicKey

  /// Seed returns the private key seed corresponding to priv. It is provided for
  /// interoperability with RFC 8032. RFC 8032's private keys correspond to seeds
  /// in this package.
  fn Seed(self) -> Slice<uint8>

  /// Sign signs the given message with priv. rand is ignored and can be nil.
  /// 
  /// If opts.HashFunc() is [crypto.SHA512], the pre-hashed variant Ed25519ph is used
  /// and message is expected to be a SHA-512 hash, otherwise opts.HashFunc() must
  /// be [crypto.Hash](0) and the message must not be hashed, as Ed25519 performs two
  /// passes over messages to be signed.
  /// 
  /// A value of type [Options] can be used as opts, or crypto.Hash(0) or
  /// crypto.SHA512 directly to select plain Ed25519 or Ed25519ph, respectively.
  fn Sign(self, rand: io.Reader, message: Slice<uint8>, opts: crypto.SignerOpts) -> Result<Slice<uint8>, error>
}

impl PublicKey {
  /// Equal reports whether pub and x have the same value.
  fn Equal(self, x: crypto.PublicKey) -> bool
}
